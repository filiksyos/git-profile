# No environment variables needed - users provide their Gemini API key through the UI
# GitHub API calls are made without authentication (rate limited to 60/hour)

# Optional: Add GitHub token for higher rate limits (5000/hour)
# GITHUB_TOKEN=your_github_token_here